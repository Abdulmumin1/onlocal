{
  "name": "proxy-worker",
  "private": true,
  "version": "1.2.0",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "wrangler": "^4.52.1"
  },
  "dependencies": {
    "hono": "^4.10.6"
  }
}